hosts:
  - localhost:8091
username: user
password: 123456
bucketName: dcp-test
dcp:
  group:
    name: groupName
    membership:
      type: dynamic
  config:
    priority: low   # low | medium | high (default: low)